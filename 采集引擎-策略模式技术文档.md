# 采集引擎技术文档 —— 策略模式框架

> 版本：v2.0
> 日期：2026-05-25
> 状态：已实现（v1.0 + v1.1）
> 上级文档：《采购舆情检测系统-技术实现文档》第六章

---

> **文档定位说明**
>
> 本文档是《采购舆情检测系统-技术实现文档》的**子文档**，专注于「多网站采集引擎」这一个模块的详细设计。
>
> - **本文档管**：采集引擎架构、策略模式设计、策略基类、引擎公共能力、全部 12 个策略详解、新增站点指南
> - **本文档不管**：主程序调度、PostgreSQL、Dify AI、钉钉推送、Docker 部署（这些在主文档中）

---

## 一、设计决策

### 1.1 为什么不用"纯配置化"

纯配置化（所有网站共用一套 CSS 选择器配置）的前提是：各网站的 HTML 结构差异只体现在选择器上。但实际采集 12 个政务/行业网站后发现，差异远不止选择器：

| 差异维度 | 具体例子 |
|----------|---------|
| 反爬机制 | NMPA 需要 Sec-Fetch-* 请求头（否则 412）；海关总署用瑞数反爬（需要无头浏览器）；CFDI 拒绝 google.com Referer |
| 请求方式 | 大部分站 HTTP GET 可用；海关总署必须 StealthyFetcher（无头浏览器模式） |
| URL 规则 | NMPA 文章 URL 含 14 位时间戳；药智网用 onclick 属性映射板块；SAMR 部分页面为 JS 渲染 |
| 内容结构 | 正文在 `#ivs_content` / `.news-con` / `div.article_html` / `#js_content` 等；药智网有 3 种页面类型 |
| 频率限制 | NMPA 需间隔 ≥5 秒；大部分站无限制 |
| 列表解析 | 大部分站标准 `<a>` 链接；食品法规中心有 3 种列表结构；药智网板块在 JS onclick 中 |

纯配置无法覆盖这些差异，强行做配置化会导致配置项爆炸，最终等于在配置文件里写代码。

### 1.2 为什么选"公共引擎 + 独立策略"

| 方案 | 优点 | 缺点 |
|------|------|------|
| 纯配置化 | 零代码新增站点 | 只能覆盖 60% 的差异，剩余 40% 还是要写代码 |
| 每站独立脚本 | 100% 灵活 | 大量重复代码（重试、日志、去重、存储） |
| **公共引擎 + 独立策略** | 公共逻辑只写一次，站点差异用代码精确控制 | 新增站点需写一个策略文件（约 50-100 行） |

核心思路：**引擎管调度、重试、日志；策略文件只管"这个站怎么抓、怎么解析"**。

---

## 二、引擎架构

```
┌─────────────────────────────────────────────────────┐
│          FastAPI main.py (APScheduler / 手动触发)      │
└──────────────────────┬──────────────────────────────┘
                       │ load_strategies()
                       ▼
┌─────────────────────────────────────────────────────┐
│              config.py — 策略加载器                    │
│  读取 config.yaml → importlib 动态导入策略类          │
│  返回 list[BaseSiteStrategy]                         │
└──────────────────────┬──────────────────────────────┘
                       │ strategies
                       ▼
┌─────────────────────────────────────────────────────┐
│              engine.py — 采集引擎                      │
│                                                      │
│  ScraperEngine.run(strategy)                         │
│    ① 调用 strategy.fetch_latest() → 文章列表          │
│    ② 逐篇调用 strategy.fetch_article(url) → 详情     │
│    ③ 返回 list[Article]                              │
│                                                      │
│  引擎提供的公共能力：                                   │
│  · http_get()：自动重试、TLS 指纹、stealthy_headers   │
│  · 请求间延迟（默认 3 秒）                              │
│  · 单篇失败不中断其他                                   │
│  · regex_find_date()：正则兜底日期提取                 │
│  · 统一日志记录                                       │
└──────────────────────┬──────────────────────────────┘
                       │ list[Article]
                       ▼
              PipelineCoordinator（主文档负责）
              去重 → 关键词匹配 → AI分析 → 入库 → 推送
```

### 引擎的职责边界

```
引擎负责（本文档）              引擎不负责（其他模块/主文档）
─────────────────             ─────────────────────────
加载策略文件                   关键词匹配（keyword_matcher.py）
调度各站点执行                 去重判断（pipeline.py）
HTTP 请求（重试/频率控制）     AI 分析（dify_client.py）
日志记录                      数据库存储（database.py）
错误隔离（单篇失败不中断）     钉钉推送（dingtalk_client.py）
结果标准化（Article 对象）     定时调度（main.py）
```

---

## 三、核心模块接口

### 3.1 数据模型 (models.py)

Article 使用 **Pydantic BaseModel**，所有采集策略返回此类型。

| 字段 | 类型 | 说明 |
|------|------|------|
| title | str (必填) | 文章标题 |
| url | str (必填) | 文章 URL |
| source | str (必填) | 来源名称 |
| source_category | str (可选) | 采集板块 |
| source_type | str | "website" / "wechat" |
| published_at | str (可选) | 原始发布时间 |
| content | str (可选) | 正文全文 |
| url_hash | str (计算属性) | URL 的 MD5（协议归一化） |

下游填充字段：matched_keywords、ai_summary、ai_category、ai_score、dingtalk_sent、error_msg

### 3.2 策略基类 (base_strategy.py)

```
BaseSiteStrategy (ABC)
├── site_name: str          # 子类必须定义
├── site_url: str           # 子类必须定义
├── fetch_latest() -> list[Article]    # 抽象方法
└── fetch_article(url) -> Article|None # 抽象方法
```

### 3.3 公共请求函数 (engine.http_get)

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| url | str | 必填 | 请求地址 |
| headers | dict | None | 自定义请求头（与默认头合并） |
| impersonate | str | "chrome" | TLS 指纹模拟 |
| retry | int | 2 | 应用层最大重试次数 |
| retry_delay | float | 5.0 | 重试间隔秒数 |
| timeout | int | 30 | 请求超时秒数 |

默认行为：
- stealthy_headers=True（Scrapling 自动添加反检测头）
- 默认 Accept 头对齐 Chrome 真实值
- 默认 Accept-Language: zh-CN,zh;q=0.9
- Scrapling 内置重试禁用（retries=1），由应用层循环统一控制

### 3.4 辅助函数 (engine.regex_find_date)

从页面文本前 3000 字符中提取日期，格式：`YYYY-MM-DD`。支持 `YYYY-MM-DD`、`YYYY年MM月DD日`、`YYYY.MM.DD` 等格式。

### 3.5 策略加载器 (config.py)

`load_strategies(config_path)` 接口：
- 读取 config.yaml，解析每站的 `strategy` 字段（格式：`sites.module:Class`）
- 使用 importlib 动态导入策略模块和类
- 将 yaml 中的 name、url、categories 等参数传给策略构造函数
- 跳过 `enabled: false` 的站点
- 返回 list[BaseSiteStrategy]

### 3.6 站点配置 (config.yaml)

每站一条配置，字段说明：

| 字段 | 必填 | 说明 |
|------|------|------|
| name | 是 | 站点中文名 |
| url | 是 | 站点首页 URL |
| strategy | 是 | 策略路径（格式：`sites.module:ClassName`） |
| enabled | 否 | 是否启用（默认 true） |
| categories | 否 | 板块列表（不填则使用策略文件内置的默认板块） |

---

## 四、全部策略详解

### 4.1 国家药监局 (NMPA)

| 属性 | 值 |
|------|-----|
| 策略文件 | `sites/nmpa.py` |
| 类名 | `NMPAStrategy` |
| site_name | 国家药监局 |
| 板块数 | 16（药品 4 + 医疗器械 6 + 化妆品 6） |
| 请求方式 | 标准 http_get |

**反爬机制**：中等。NMPA 检查 `Sec-Fetch-*` Fetch Metadata 请求头，缺失时返回 HTTP 412。必须通过 `NMPA_HEADERS` 传入 `Sec-Fetch-Dest: document`、`Sec-Fetch-Mode: navigate`、`Sec-Fetch-Site: none`、`Sec-Fetch-User: ?1`、`Upgrade-Insecure-Requests: 1`。请求间隔需 ≥5 秒（`retry_delay=5`）。

**URL 规律**：文章 URL 路径包含 14 位时间戳 + `.html` 后缀，如 `20260415012345678.html`。列表页返回相对路径（`../../xxgk/xxx.html` 格式），需要 URL 解析处理。

**内容选择器**（按优先级）：`#ivs_content` → `.Custom_UnionStyle` → `.article-content` → `.TRS_Editor` → `.text-content` → `.content`

**特殊处理**：政策解读类文章可能为图片海报，正文无文字可提取。

---

### 4.2 上海药监局 (ShYJJ)

| 属性 | 值 |
|------|-----|
| 策略文件 | `sites/sh_yjj.py` |
| 类名 | `ShanghaiYJJStrategy` |
| site_name | 上海药监局 |
| 板块数 | 6（最新公开信息 4 + 监管信息 2） |
| 请求方式 | 标准 http_get |

**反爬机制**：无。标准 HTTP GET 可正常访问。

**URL 规律**：文章链接以 `.html` 结尾，匹配含 `\20` 的 URL 模式。

**内容选择器**：`#ivs_content` → `.Custom_UnionStyle` → `.TRS_Editor` → `.content`。标题：`#ivs_title`。日期：`small.PBtime`。

---

### 4.3 国家卫生健康委员会 (NHC)

| 属性 | 值 |
|------|-----|
| 策略文件 | `sites/nhc.py` |
| 类名 | `NHCStrategy` |
| site_name | 国家卫生健康委员会 |
| 板块数 | 6（政策法规 3 + 财政管理 1 + 建议提案 2） |
| 请求方式 | 标准 http_get |

**反爬机制**：无。标准 HTTP GET 可正常访问。

**内容选择器**：`div.con` → `div#xw_box` → `#ivs_content` → `.Custom_UnionStyle` → `.TRS_Editor` → `.content`

---

### 4.4 国家食品药品审核查验中心 (CFDI)

| 属性 | 值 |
|------|-----|
| 策略文件 | `sites/cfdi.py` |
| 类名 | `CFDIStrategy` |
| site_name | 国家食品药品审核查验中心 |
| 板块数 | 10（最新消息 1 + 通知公告 1 + 工作动态 1 + 政策法规 3 + 检查专栏 4） |
| 请求方式 | 标准 http_get |

**反爬机制**：中等。Scrapling 的 `stealthy_headers=True` 默认会将 Referer 设为 `google.com`，CFDI 服务器会拒绝来自 google.com 的引用，返回 403。必须通过 `CFDI_HEADERS` 显式设置 `Referer: https://www.cfdi.org.cn/cfdi/`。

**内容选择器**：`div.news-con` → `.content` → `#ivs_content`

---

### 4.5 海关总署 (Customs)

| 属性 | 值 |
|------|-----|
| 策略文件 | `sites/customs.py` |
| 类名 | `CustomsStrategy` |
| site_name | 海关总署 |
| 板块数 | 10（新闻发布 7 + 政务公开 3） |
| 请求方式 | **StealthyFetcher**（无头浏览器模式） |

**反爬机制**：高。海关总署使用**瑞数 (RuiShu) 反爬**，标准 HTTP 请求返回 412 + 加密 JS 挑战。必须使用 Scrapling 的 StealthyFetcher（基于 Camoufox/Firefox 的无头浏览器）绕过。同时需设置 `google_search=False`，因为默认 Referer=google.com 会导致 412。

**跨域采集**："国务院要闻"板块的文章链接指向 `gov.cn`（跨域），StealthyFetcher 可直接处理。

**内容选择器**：`div.article_detail` → `.content` → `#ivs_content`。标题通过 `meta[name="ArticleTitle"]` 提取，日期通过 `meta[name="PubDate"]` 提取。

**性能说明**：StealthyFetcher 启动较慢（需要启动浏览器进程），但总请求量约 20 次，可接受。

---

### 4.6 国家市场监督管理局 (SAMR)

| 属性 | 值 |
|------|-----|
| 策略文件 | `sites/samr.py` |
| 类名 | `SAMRStrategy` |
| site_name | 国家市场监督管理局 |
| 板块数 | 4（司局动态 3 + 政策文件 1） |
| 请求方式 | 标准 http_get |

**反爬机制**：低。裸 curl 返回 403，但 Scrapling 的 `impersonate=chrome` 可以正常访问。

**特殊处理**：
1. **页面缓存去重**：3 个"司局动态"子板块共享同一个列表页 URL，引擎内置页面缓存避免重复请求。
2. **JS 渲染页面**："政策文件"板块（zcwj）的列表页为 JS 动态渲染，无法直接抓取。策略中使用 `_find_zcwj_latest` 方法进行回退处理。

**内容选择器**：`#ivs_content` → `.Custom_UnionStyle` → `.TRS_Editor` → `div.con` → `div.content`

---

### 4.7 食品资讯中心 (Foodmate)

| 属性 | 值 |
|------|-----|
| 策略文件 | `sites/foodmate.py` |
| 类名 | `FoodmateStrategy` |
| site_name | 食品资讯中心 |
| 板块数 | 10（中国食品、权威发布、国际食品、国际预警、食品科技、外讯导读、产经企业、会展动态、食品专题、食品网刊） |
| 请求方式 | 标准 http_get |

**反爬机制**：无。标准 HTTP GET 可正常访问。

**内容选择器**：`div#article` → `div.content` → `#ivs_content` → `.TRS_Editor`

---

### 4.8 食品法规中心 (FoodmateLaw)

| 属性 | 值 |
|------|-----|
| 策略文件 | `sites/foodmate_law.py` |
| 类名 | `FoodmateLawStrategy` |
| site_name | 食品法规中心 |
| 板块数 | 9（政策法规 4 + 法规草案 3 + 法规解读 1 + 法规动态 1） |
| 请求方式 | 标准 http_get |

**反爬机制**：无。标准 HTTP GET 可正常访问。

**特殊处理**：不同板块的列表页 HTML 结构不同，策略内置 3 种列表解析逻辑：
1. 推荐类板块：解析特定推荐区域
2. 带 `alt` 属性的链接板块
3. 通用 `a[href*="show-"]` 链接板块

**内容选择器**：`div#article` → `div.content` → `#ivs_content` → `.TRS_Editor`

---

### 4.9 进出口食品安全信息平台 (Exim)

| 属性 | 值 |
|------|-----|
| 策略文件 | `sites/exim.py` |
| 类名 | `EximStrategy` |
| site_name | 进出口食品安全信息平台 |
| 板块数 | 7（最新信息动态 + 6 个国家/地区：美国、欧盟、澳大利亚、日本、韩国、香港） |
| 请求方式 | 标准 http_get |

**反爬机制**：无。标准 HTTP GET 可正常访问。

**内容选择器**：`div#article` → `div.content` → `#ivs_content` → `.TRS_Editor`

---

### 4.10 每日食品网 (Foodaily)

| 属性 | 值 |
|------|-----|
| 策略文件 | `sites/foodaily.py` |
| 类名 | `FoodailyStrategy` |
| site_name | 每日食品网 |
| 板块数 | 1（快讯） |
| 请求方式 | 标准 http_get |

**反爬机制**：低。需要浏览器 User-Agent，裸 curl 返回 403。Scrapling 的 `impersonate=chrome` 可正常访问。

**特殊处理**：详情页只有相对日期（如"X 小时前"），因此从列表页的 `div.date-card`（含 `span.month` + `span.day`）提取日期。

**内容选择器**：`div.message pre` → `div.message` → `div.content`。标题：`a.title`。

---

### 4.11 药智网 (Yaozh)

| 属性 | 值 |
|------|-----|
| 策略文件 | `sites/yaozh.py` |
| 类名 | `YaozhStrategy` |
| site_name | 药智网 |
| 板块数 | 4（新闻、会议、医械、前沿） |
| 请求方式 | 标准 http_get |

**反爬机制**：无。SSR 页面，标准 HTTP GET 可正常访问。

**特殊处理**：
1. **板块识别**：板块不是通过 URL 区分，而是通过页面按钮的 `onclick` 属性映射板块名称。
2. **三种页面类型**：详情页有 3 种不同结构，策略根据 URL 路径自动选择解析方式：
   - 存档页（`news.yaozh.com/archive/`）：选择器 `div.article_html_control` → `div.article_html`
   - 会议页（`news.yaozh.com/meeting/detail/`）：选择器 `div.fl.mainShow` → `div.info_content`
   - 微信重定向页：选择器 `#js_content` → `div.rich_media_content`

---

### 4.12 植提桥 (Herbridge)

| 属性 | 值 |
|------|-----|
| 策略文件 | `sites/herbridge.py` |
| 类名 | `HerbridgeStrategy` |
| site_name | 植提桥 |
| 板块数 | 1（固定列表 URL，无 CATEGORIES 字典） |
| 请求方式 | 标准 http_get |

**反爬机制**：无。SSR 页面，标准 HTTP GET 可正常访问。

**内容选择器**：列表：`div.list-box` → `div.subject h6 a`。详情标题：`div.content div.title`。详情内容：`div.content div.main`。日期：`span.date`。

---

## 五、策略对比总览

| 策略 | 站点 | 板块数 | 请求方式 | 自定义头 | 反爬等级 | 特殊处理 |
|------|------|--------|---------|---------|---------|---------|
| NMPAStrategy | 国家药监局 | 16 | http_get | Sec-Fetch-* | 中 | 412 防御，5 秒间隔 |
| ShanghaiYJJStrategy | 上海药监局 | 6 | http_get | 无 | 无 | — |
| NHCStrategy | 国家卫健委 | 6 | http_get | 无 | 无 | — |
| CFDIStrategy | 审核查验中心 | 10 | http_get | Referer | 中 | 拒绝 google Referer |
| CustomsStrategy | 海关总署 | 10 | StealthyFetcher | 无 | **高** | 瑞数反爬，无头浏览器 |
| SAMRStrategy | 市场监管总局 | 4 | http_get | 无 | 低 | 页面缓存去重，JS 渲染回退 |
| FoodmateStrategy | 食品资讯中心 | 10 | http_get | 无 | 无 | — |
| FoodmateLawStrategy | 食品法规中心 | 9 | http_get | 无 | 无 | 3 种列表解析逻辑 |
| EximStrategy | 进出口食品安全 | 7 | http_get | 无 | 无 | — |
| FoodailyStrategy | 每日食品网 | 1 | http_get | 无 | 低 | 列表页提取日期 |
| YaozhStrategy | 药智网 | 4 | http_get | 无 | 无 | 3 种页面类型，onclick 板块映射 |
| HerbridgeStrategy | 植提桥 | 1 | http_get | 无 | 无 | 固定 URL |

---

## 六、新增站点操作指南

新增一个网站采集策略分为 **探索 → 验证 → 编码 → 注册** 四个阶段。

### Phase 1：探索阶段

**目标**：搞清楚这个站能不能抓、怎么抓。

**操作**：用 Scrapling 尝试抓取目标网站列表页和详情页，观察以下信息：

| 关注项 | 提取什么 | 为什么重要 |
|--------|---------|-----------|
| HTTP 状态码 | 200 / 412 / 403 | 决定用哪种请求方式 |
| 成功方式 | http_get 还是 StealthyFetcher | 决定策略中调用哪个 |
| 必需请求头 | 哪些 headers 加上后才返回 200 | 写入策略文件的 `XXX_HEADERS` |
| 频率表现 | 连续请求几次后是否触发限制 | 决定 `retry_delay` |
| 页面结构 | 文章链接在哪些标签里 | 决定 fetch_latest 的解析逻辑 |
| URL 格式 | 时间戳、递增 ID 还是 slug | 决定链接识别规则 |
| 正文容器 | 内容在哪个 CSS 选择器里 | 决定 fetch_article 的选择器 |

**反爬降级策略**：

```
http_get（默认）
  │ 412/403 → 加自定义 headers 重试
  │ 仍失败 → StealthyFetcher（无头浏览器）
  │ 仍失败 → 该站可能需要代理或 Cookie，当前框架暂不支持
```

**Phase 1 产出**：反爬调研笔记，包含请求方式、必需头、频率阈值、URL 规律、内容选择器。

### Phase 2：验证阶段

**目标**：确认探索结果可稳定复现。

**验证要点**：
- 列表页和详情页稳定返回 200
- 连续请求不触发反爬
- 正文内容非空（排除纯图片页面）
- 标题、日期能正确提取

### Phase 3：编码阶段

**操作**：创建策略文件，继承 BaseSiteStrategy，实现 fetch_latest 和 fetch_article。

**策略文件结构**：

```
sites/your_site.py
├── SITE_HEADERS = {...}        # 自定义请求头（如不需要则不定义）
├── class YourSiteStrategy(BaseSiteStrategy):
│   ├── site_name = "站点名"
│   ├── site_url = "https://..."
│   ├── CATEGORIES = {...}      # 板块名称 → 列表页 URL
│   ├── __init__(**kwargs)      # 处理 config.yaml 传入的 categories 参数
│   ├── fetch_latest()          # 解析列表页 → Article 列表
│   ├── fetch_article(url)      # 解析详情页 → 完整 Article
│   └── _resolve_url(href, base) # 相对 URL 解析
```

**策略文件编写要点**：

必须写的（每个站都不同）：
- `site_name` / `site_url` / `CATEGORIES`
- `fetch_latest()` 中的链接识别和解析逻辑
- `fetch_article()` 中的内容提取选择器
- 自定义请求头（如需要）

不需要写的（引擎统一处理）：
- 重试逻辑（http_get 自带）
- 请求间隔（ScraperEngine 控制）
- 日志记录（logger 自动输出）
- 错误隔离（引擎 try/except 包裹）
- 去重、存储、推送（下游管道负责）

### Phase 4：注册与测试

**注册**：在 config.yaml 添加一条配置。

```yaml
sites:
  - name: 站点中文名
    url: https://xxx.gov.cn
    strategy: sites.your_site:YourSiteStrategy
    enabled: true
    categories:
      - 板块A
      - 板块B
```

**测试**：手动触发单站采集，验证结果。

```bash
curl -X POST http://localhost:8000/api/collect/站点中文名
```

**确认清单**：
- [ ] 每个板块都拿到了最新文章
- [ ] 文章标题正确（非乱码、非空）
- [ ] 发布日期已提取
- [ ] 正文内容非空
- [ ] 无 412/403 错误
- [ ] 无异常堆栈

---

## 七、设计约束

1. **策略文件之间互不依赖** — 删除或修改一个策略不影响其他站点
2. **公共逻辑只改一处** — 重试策略、日志格式等改动只修改 engine.py
3. **策略文件不依赖数据库** — 策略只负责"抓取和解析"，不负责"存储和推送"
4. **每个策略文件控制在 100 行以内** — 超过说明该站点逻辑过于复杂，考虑拆分子板块
5. **config.yaml 是唯一的注册入口** — 不注册的站点不会被加载，方便临时禁用
6. **引擎返回 Article 对象** — 不返回 dict，所有下游模块统一使用 Article 模型
7. **请求统一走 http_get** — 策略文件不直接使用 Scrapling Fetcher（海关总署除外，使用 StealthyFetcher）

---

*v2.0 — 全量重写：状态更新为已实现，新增全部 12 个策略详解和对比总览表，更新接口为实际代码*
*最后更新：2026-05-25*
